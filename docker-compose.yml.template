version: "3"
services:
  postgis:
    image: pgrouting/pgrouting:v%%PGROUTING_VERSION%%-postgresql_%%PG_MAJOR%%
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=postgres
volumes:
  db-data:
