version: "3"
services:
  postgis:
    image: pgrouting/pgrouting:pg10_3.0.0-dev
    ports:
      - "5432:5432"
    volumes:
      - db-data:/data
    environment:
      - POSTGRES_PASSWD=postgres
volumes:
  db-data:
